{
  "name": "tailwind_starter",
  "version": "1.0.0",
  "description": "- Tailwwind is a package available in npm so you can start by creating a packge.json file do:\r     npm init -y\r - you need to install tawilwindcss and postcss-cli and autoprefixer\r     npm install tailwondcss postcss-cli autoprefixer\r - now you need to configure tailwind by creating a tailwind.config.js file using:\r     npx tailwind init\r - then you need to configure postcss by creating a postcss.config.js file thats include the plugins you want to use require('tailwindcss), require(autoprefixer)\r - then you create a css file so that tailwind can look into it and create your fianl css based on it\r     - in it define",
  "main": "index.js",
  "scripts": {
    "build": "postcss css/tailwind.css -o public/build/tailwind.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^9.6.1",
    "postcss-cli": "^6.1.3",
    "tailwindcss": "^1.0.5"
  }
}
